[project]
name = "fraud-detection-ml"
version = "0.1.0"
description = "Multi-signal fraud detection library for in-process backend integration"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "joblib>=1.4",
    "kagglehub>=1.0.2",
    "networkx>=3.6.1",
    "pandas>=3.0.5",
    "pydantic>=2.9",
    "pytest>=9.1.1",
    "scikit-learn>=1.9.0",
    "xgboost>=3.4.1",
]
